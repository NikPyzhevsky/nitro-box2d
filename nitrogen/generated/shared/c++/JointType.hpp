///
/// JointType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitrobox2d {

  /**
   * An enum which can be represented as a JavaScript enum (JointType).
   */
  enum class JointType {
    DISTANCE      SWIFT_NAME(distance) = 0,
    FILTER      SWIFT_NAME(filter) = 1,
    MOTOR      SWIFT_NAME(motor) = 2,
    MOUSE      SWIFT_NAME(mouse) = 3,
    PRISMATIC      SWIFT_NAME(prismatic) = 4,
    REVOLUTE      SWIFT_NAME(revolute) = 5,
    WELD      SWIFT_NAME(weld) = 6,
    WHEEL      SWIFT_NAME(wheel) = 7,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitrobox2d

namespace margelo::nitro {

  // C++ JointType <> JS JointType (enum)
  template <>
  struct JSIConverter<margelo::nitro::nitrobox2d::JointType> final {
    static inline margelo::nitro::nitrobox2d::JointType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      int enumValue = JSIConverter<int>::fromJSI(runtime, arg);
      return static_cast<margelo::nitro::nitrobox2d::JointType>(enumValue);
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitrobox2d::JointType arg) {
      int enumValue = static_cast<int>(arg);
      return JSIConverter<int>::toJSI(runtime, enumValue);
    }
    static inline bool canConvert(jsi::Runtime&, const jsi::Value& value) {
      if (!value.isNumber()) {
        return false;
      }
      double number = value.getNumber();
      int integer = static_cast<int>(number);
      if (number != integer) {
        // The integer is not the same value as the double - we truncated floating points.
        // Enums are all integers, so the input floating point number is obviously invalid.
        return false;
      }
      // Check if we are within the bounds of the enum.
      return integer >= 0 && integer <= 7;
    }
  };

} // namespace margelo::nitro
